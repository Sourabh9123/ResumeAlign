name: Deploy to EC2

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy application to EC2
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            mkdir -p ~/apps/resumeai
            cd ~/apps/resumeai

            if [ ! -d .git ]; then
              git clone git@github.com:Sourabh9123/ResumeAlign.git .
            fi

            git pull origin main

            make deploy
