\documentclass[11pt,a4paper]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{geometry}
\usepackage{enumitem}
\usepackage{hyperref}
\usepackage{xcolor}

% Color definitions
\definecolor{primary}{RGB}{30, 64, 175}

\geometry{left=1.5cm, top=1.5cm, right=1.5cm, bottom=1.5cm, footskip=.5cm}
\pagestyle{empty}

% Custom section command
{% raw %}
\newcommand{\resumesection}[1]{
    \vspace{8pt}
    {\color{primary}\Large\textbf{#1}}
    \vspace{2pt}
    \hrule
    \vspace{6pt}
}
{% endraw %}

\begin{document}
\begin{center}
    {\Huge\textbf{ {{ resume.personal_info.name | default('Name Not Provided') }} }} \\[6pt]
    {{ resume.personal_info.email | default('') }} $|$ 
    {{ resume.personal_info.phone | default('') }} $|$ 
    {{ resume.personal_info.location | default('') }}
    {% if resume.personal_info.links and resume.personal_info.links|length > 0 %}
    \par \vspace{3pt}
    {% for link in resume.personal_info.links %}
        \href{ {{- link -}} }{ {{- link | replace('https://', '') | replace('http://', '') | replace('_', '\\_') -}} }{% if not loop.last %} $|$ {% endif %}
    {% endfor %}
    {% endif %}
\end{center}

{% if resume.summary %}
\resumesection{Professional Summary}
\noindent {{ resume.summary }}
{% endif %}

{% if resume.experience and resume.experience|length > 0 %}
\resumesection{Experience}
{% for exp in resume.experience %}
\noindent
\textbf{ {{ exp.company }} } \hfill {{ exp.start_date }} -- {{ exp.end_date }} \\
\textit{ {{ exp.title }} } \\
\vspace{-4pt}
\begin{itemize}[leftmargin=*, noitemsep, topsep=0pt]
    \item {{ exp.description }}
\end{itemize}
\vspace{4pt}
{% endfor %}
{% endif %}

{% if resume.education and resume.education|length > 0 %}
\resumesection{Education}
{% for edu in resume.education %}
\noindent
\textbf{ {{ edu.institution }} } \hfill {{ edu.graduation_date }} \\
\textit{ {{ edu.degree }} } \\
\vspace{4pt}
{% endfor %}
{% endif %}

{% if resume.skills and resume.skills|length > 0 %}
\resumesection{Skills}
\noindent {{ resume.skills | join(', ') }}
{% endif %}

\end{document}
